package com.example.onehealth.repository;

import com.example.onehealth.entity.MedServ;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MedServRepository extends JpaRepository<MedServ, Integer> {
}